package seedu.duke.commands;

import seedu.duke.universities.UniversityList;

public class RemoveUniCommand extends Command {
    public RemoveUniCommand(int index, UniversityList universitySelectedList) {
    }
}
