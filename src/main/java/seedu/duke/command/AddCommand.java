package seedu.duke.command;

import java.util.Map;

public class AddCommand extends Command {
    public AddCommand(Map<String, String> commandOptions) {
    }

    @Override
    public CommandResult executeCommand() throws Exception {
        return null;
    }
}
