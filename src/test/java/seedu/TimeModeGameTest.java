package seedu;

import org.junit.jupiter.api.Test;
import seedu.typists.game.TimeModeGame;

import java.util.ArrayList;
import java.util.Arrays;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class TimeModeGameTest {
    private static final String TEXT_TXT = "this is test text";
    //name: whatIsBeingTested_descriptionOfTestInputs_expectedOutcome

    @Test
    public void wordLines_sampleTxt_correct() {
        //TimeModeGame g = new TimeModeGame(TEXT_TXT, 15);
        //should get an error cause the text is not enough words
        //need update
    }
}
